// This file ensures that Safe Area Context is properly initialized
// It should be imported early in the app lifecycle

import { SafeAreaProvider } from 'react-native-safe-area-context';

// Export the provider for use in the app
export { SafeAreaProvider };

export default {
  // This is just a placeholder to ensure the file is imported
  initialized: true
}; 